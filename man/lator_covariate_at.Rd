% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{lator_covariate_at}
\alias{lator_covariate_at}
\title{Resolve a longitudinal covariate at specified times}
\usage{
lator_covariate_at(
  patient,
  name,
  times,
  method = c("locf", "linear", "nearest", "none"),
  max_age = Inf,
  max_gap = Inf,
  fallback = NULL,
  fallback_unit = "",
  uncertainty_sd = NA_real_,
  cutoff = Inf
)
}
\arguments{
\item{patient}{A `lator_patient`.}

\item{name}{Covariate event name.}

\item{times}{Numeric timeline hours.}

\item{method}{One of `"locf"`, `"linear"`, `"nearest"`, or `"none"`.}

\item{max_age}{Maximum permitted age in hours for carried or nearest values.}

\item{max_gap}{Maximum interval in hours spanning a linear interpolation.}

\item{fallback}{Optional explicit fallback value. It is labelled `fallback`,
never as an observation.}

\item{fallback_unit}{Unit for `fallback` if no observed unit is available.}

\item{uncertainty_sd}{Optional standard deviation attached to fallback
values. Event-specific `metadata$uncertainty_sd` takes precedence for
observed values.}

\item{cutoff}{Ignore evidence recorded after this patient-timeline time.}
}
\value{
A provenance-rich data frame with one row per requested time.
}
\description{
Missing observations are never silently converted into population values.
Carry-forward, interpolation, nearest-neighbour, and fallback behaviour are
explicit policies and every returned value records its provenance and age.
}
